#include<iostream>
#include<math.h>


 using namespace std;

    int main(void) {
   /**  int N,Q,Num=0;
     cin>>N>>Q;
     int *Ary = new int[N];
     for(int i=0;i<N;i++) cin>>Ary[i];
     for (int i=0;i<Q;i++) {
     int temp;cin>>temp;
     if(temp!=0) {
       int x;cin>>x;
       Ary[x-1]==1;
      }
     else {
       int l,r,n=0;
       cin>>l>>r;
       for (;l<=r;l++) {
          if(Ary[l]!=0) Num+=pow(2,n);
          n++;
      }
     }
    }
    (Num%2!=1) ? cout<<"EVEN" : cout<<"ODD";

     delete Ary;

     return 0;*/
     char i=0;
     int ch,c=0;
     cin>>ch;
     //for (i=0;i>ch;i++) { cout<<(int)i;c++; }
     if(i>=ch)
     cout<<(int)i;

    }
